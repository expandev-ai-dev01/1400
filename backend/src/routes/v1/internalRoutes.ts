/**
 * @summary
 * Internal (authenticated) API routes configuration
 *
 * @module routes/v1/internalRoutes
 *
 * @description
 * Defines all protected API endpoints that require authentication.
 * These routes are only accessible to authenticated users.
 */

import { Router } from 'express';

const router = Router();

/**
 * @remarks
 * Internal routes will be added here as features are implemented.
 * All routes in this file should include authentication middleware.
 */

export default router;
